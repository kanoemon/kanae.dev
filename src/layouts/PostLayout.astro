---
import Layout from './Layout.astro';
import Tag from '../components/Tag.astro';

const allPosts = await Astro.glob('../pages/posts/*.md');
allPosts.sort((a, b) => Date.parse(b.frontmatter.pubDate) - Date.parse(a.frontmatter.pubDate));

const { frontmatter } = Astro.props;

interface PaginationPosts {
	next: object;
	current: object;
	prev: object;
}

let posts: PaginationPosts = {
	next: {},
	current: {},
	prev: {},
};

allPosts.forEach((post, index) => {
	if (post.frontmatter.title == frontmatter.title) {
		if (allPosts[index + 1]) {
			posts.prev = allPosts[index + 1];
		}
		if (allPosts[index - 1]) {
			posts.next = allPosts[index - 1];
		}
		posts.current = allPosts[index];
	}
});

---

<Layout title={frontmatter.title} type="article">
	<article>
		<header class="py-9">
			<div class="flex">
				<time class="text-sm">{new Date(frontmatter.pubDate).toLocaleDateString()}</time>
				<div class="text-sm ml-3">
					{frontmatter.tags.map((tag: any) => (
						<Tag tag={tag} />
					))}
				</div>
			</div>
			<h1 class="py-4 text-lg font-semibold lg:text-3xl">
				{frontmatter.title}
			</h1>
			{frontmatter.thumbnail ? 
			<img src={frontmatter.thumbnail} class="pt-12">
			: null }
		</header>
		<section class="mt-9 prose max-w-none">
			<slot />
		</section>
			<div class="my-4">
				<ul class="flex">
					<li class="pr-3"><a href="https://b.hatena.ne.jp/entry/" class="hatena-bookmark-button" data-hatena-bookmark-layout="basic-label-counter" data-hatena-bookmark-lang="ja" title="このエントリーをはてなブックマークに追加"><img src="https://b.st-hatena.com/images/v4/public/entry-button/button-only@2x.png" alt="このエントリーをはてなブックマークに追加" width="20" height="20" style="border: none;" /></a><script type="text/javascript" src="https://b.st-hatena.com/js/bookmark_button.js" charset="utf-8" async="async"></script></li>
					<li><a href="https://twitter.com/share?ref_src=twsrc%5Etfw" class="twitter-share-button" data-show-count="false">Tweet</a><script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script></li>
					</ul>
			</div>
		<footer class="mt-8">
			<nav class="flex justify-between py-12 flex-wrap">
				{posts.prev.url ? 
				<div>
					<i class="fa fa-angle-left" aria-hidden="true"></i>
					<a href={posts.prev.url} class="border-b-4 border-[#FCC5D3]">{posts.prev.frontmatter.title}</a>
				</div>
				: null }
				{posts.next.url ? 
				<div>
					<a href={posts.next.url} class="border-b-4 border-[#FCC5D3]">{posts.next.frontmatter.title}</a>
					<i class="fa fa-angle-right" aria-hidden="true"></i>
				</div>
				: null }
			</nav>
		</footer>
	</article>
</Layout>

<style>
</style>
